# OS
.DS_Store

# Linked git worktrees (parallel agent work)
.worktrees/

# Local screenshot scratch (playwright)
.screenshots/

# Node / Next.js
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# Env and local config
.env
.env.*
!.env.example

# Logs and caches
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.turbo/
coverage/
.vercel
*.pem

# Engine-sync scratch (scripts/sync-engine.mjs clones upstream here)
.engine-sync/

# NOTE: casework/ is deliberately COMMITTED in this private repo (research
# briefs live under casework/<case-slug>/ — see casework/README.md). Do not
# re-add a casework/ ignore rule copied from a public-repo setup.
