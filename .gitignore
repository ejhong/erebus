# OS
.DS_Store

# Linked git worktrees (parallel agent work)
.worktrees/

# Local screenshot scratch (playwright)
.screenshots/

# Node / Next.js
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# Env and local config
.env
.env.*
!.env.example

# Logs and caches
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.turbo/
coverage/
.vercel
*.pem

# Engine-sync scratch (scripts/sync-engine.mjs clones upstream here)
.engine-sync/

# Research briefs are held privately OUTSIDE this repository and must never
# be committed to it: the repo is public, and briefs routinely carry
# third-party copyrighted material kept as discovery aids plus material
# about living persons that the living-persons rules deliberately keep off
# the site. This rule replaced the opposite instruction on 2026-08-26, when
# the repo went public and casework/ was purged from history — see
# docs/DECISIONS.md and scripts/purge-casework-history.sh.
casework/
