## Assessment refresh

- **orch-or**: new AI assessment overlay (run 2026-08-25-auto-jlsi, verdict unresolved). What changed since the previous run: The new material is all on the rival explanation, not on Orch OR: the classical account has been split into a narrow claim (ORCH-C003) that still stands on ORCH-E015, and a newly assessed exhaustive claim (ORCH-C004) graded unresolved, because ORCH-E030 shows the β3(N265M) effect is complete only for immobility and ORCH-E031 reports no channel or combination sufficient even for that endpoint. ORCH-E032 adds the first human observation to the stabilizer line but is retrospective with a weak surrogate endpoint, so ORCH-C012 stays mixed, and ORCH-E034 is a proponent editorial that adds no weight to ORCH-C025. Nothing new touches ORCH-C010, so the case verdict and the location of the load remain unchanged.
-   - editorial audit found nothing in the article or research agenda that the updated ledger contradicts (1 proposed edit rejected as unsafe — see the run log).

These overlays are append-only AI drafts (`humanReviewed: false`); the site labels them as such. Reverting a run = deleting its overlay file.
